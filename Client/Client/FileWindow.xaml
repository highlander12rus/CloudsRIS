<Page x:Class="Client.FileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Файлы и папки"  Height="350" Width="525">
    <Grid>
        <Label Name="CurFolder" Content=" Текущая директория:" HorizontalAlignment="Left" Height="320" VerticalAlignment="Top" Width="517" Margin="-2,0,0,0"/>
        <GroupBox Header="Файлы и папки в директории" Height="250" Margin="10,30,57,0" Name="groupBox1" VerticalAlignment="Top">
            <Grid>
                <Grid.Resources>
                    <Style x:Key="Itemstyle" TargetType="{x:Type ListViewItem}">
                        <EventSetter Event="MouseDoubleClick" Handler="HandleDoubleClick" />
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="435*"/>
                </Grid.ColumnDefinitions>
                <ListView Height="225" Margin="0,3,1,0" ItemContainerStyle="{StaticResource Itemstyle}" Name="filesfolderslistView" VerticalAlignment="Top"  BorderBrush="#FF40C01D" Grid.Column="1" >

                    <!--context  Menu-->
                    <ListView.ContextMenu>
                        <ContextMenu Name="ZoneIformationList" StaysOpen="true" Background="WhiteSmoke">
                            <ContextMenu.BitmapEffect>
                                <BitmapEffectGroup/>
                            </ContextMenu.BitmapEffect>
                            <MenuItem Header="Добавить папку" Name="AddFolderContextMenu"  Click="AddFolderContextMenu_OnClick"/>
                            <MenuItem Header="Добавить файл" Name="AddFileContextMenu" Click="AddFileContextMenu_OnClick"/>
                            <MenuItem Header="Удалить папку" Name="DeleteContextMenu" Click="DeleteContextMenu_OnClick"/>
                            <MenuItem Header="Скачать файл" Name="DownloadFileContextMenu" Click="DownloadFileContextMenu_OnClick"/>
                        </ContextMenu>
                    </ListView.ContextMenu>

                    <!--Bind Item-->
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Имя" 
                                   Width="300" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Тип"  
                                   Width="110" DisplayMemberBinding="{Binding Type}" />
                            
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </GroupBox>
        <Label Name ="CurState" Content="Строка состояния:" HorizontalAlignment="Left" Margin="0,280,0,0" VerticalAlignment="Top" Height="25"/>
        <Label Name="LabelNewName"  Visibility="Hidden" Content="Введите имя папки/файла и нажмите Enter:" HorizontalAlignment="Left" Margin="0,27,0,0" VerticalAlignment="Top"/>
        <TextBox Name="TextBoxNewName" HorizontalAlignment="Left" Height="23" Visibility="Hidden" Margin="269,30,0,0" TextWrapping="Wrap" Text="Имя файла или папки" VerticalAlignment="Top" Width="224" KeyUp="TextBoxNewName_KeyUp" />
        <Label Name="State" Content="" HorizontalAlignment="Left" Margin="130,279,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.375,0.038"/>
    </Grid>
</Page>
